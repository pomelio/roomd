import ext.HttpContext as ctx;
import std.string as str;


let ppath = ctx.path();

if str.ends_with(ppath, '.md') {
  ctx.dispatch('/markdown', {});
} else if str.starts_with(ppath. '/docs') {
  ctx.download('/download', {});
} else {
  ctx.dispatch(ppath, {});
}



